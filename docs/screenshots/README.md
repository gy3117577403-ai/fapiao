# 截图占位说明

当前目录用于保存发布说明截图。Phase 6 没有生成真实截图文件，以避免为了截图保留测试报销单或暴露本地环境信息。

建议后续截图文件：

- `dashboard.png`：总览驾驶舱
- `reports.png`：报销单工作台
- `report-new.png`：新建报销单
- `report-edit.png`：编辑报销单
- `report-print.png`：打印预览

截图前请确认：

- 不显示本地绝对路径。
- 不显示数据库路径。
- 不显示私有 API 地址、token 或密钥。
- 如创建测试报销单，截图完成后必须删除并确认列表恢复原状态。
